const { createOpenAI } = require("@ai-sdk/openai"); const { generateText } = require("ai"); require("dotenv").config(); async function test() { console.log("Testing with key:", process.env.QWEN_API_KEY?.substring(0, 10) + "..."); const qwen = createOpenAI({ apiKey: process.env.QWEN_API_KEY || "", baseURL: "https://dashscope.aliyuncs.com/compatible-mode/v1", }); try { const model = qwen("qwen-turbo"); console.log("Model created, testing API call..."); const response = await generateText({ model, prompt: "Hello, this is a test. Respond with Test successful if you receive this message.", maxTokens: 50, }); console.log("API Response:", response.text); } catch (error) { console.error("Error:", error.message); } } test();
